{
  "name": "@openagents/connector-teams",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "description": "Microsoft Teams connector for Open Agents. Posts channel and chat messages via Microsoft Graph using the active tenant's delegated MSAL token.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./augment": {
      "types": "./dist/augment.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run build -w @openagents/agent-sdk && tsc -p tsconfig.json",
    "typecheck": "npm run build -w @openagents/agent-sdk && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@openagents/agent-sdk": "0.1.5"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "~6.0.3"
  },
  "engines": {
    "node": ">=20"
  },
  "sideEffects": false
}
